package com.urbanaura.urbanaura.repository;

import com.urbanaura.urbanaura.model.QuickCommerce;
import org.springframework.data.jpa.repository.JpaRepository;

public interface QuickCommerceRepository extends JpaRepository<QuickCommerce, Long> {
}
