package com.urbanaura.urbanaura.repository;

import com.urbanaura.urbanaura.model.SmartMetric;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SmartMetricRepository extends JpaRepository<SmartMetric, Long> {
}
